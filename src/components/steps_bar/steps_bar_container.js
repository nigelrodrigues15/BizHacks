import { connect } from "react-redux";
import StepsBar from "./steps_bar_1";

const mapStateToProps = (state, ownprops) => {
    return {

    };
};

const mapDispatchToProps = dispatch => {
    return {

    };
};

export default connect(
    mapStateToProps,
    mapDispatchToProps
)(StepsBar);